[build]
  publish = "public"
  command = "echo 'No build needed - static site'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
```

### File 3: `public/_redirects` (Inside public folder)
```
/*    /index.html   200